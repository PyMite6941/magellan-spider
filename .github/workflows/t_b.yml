name: TB
on:
  workflow_dispatch:
    inputs:
      batch_size:
        description: 'Topics to crawl this run'
        required: false
        default: '10'
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - run: echo b
